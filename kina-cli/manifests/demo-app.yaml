---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kina-demo-app
  namespace: default
  labels:
    app: kina-demo-app
spec:
  replicas: 4
  selector:
    matchLabels:
      app: kina-demo-app
  template:
    metadata:
      labels:
        app: kina-demo-app
    spec:
      containers:
      - name: nginx
        image: nginx:alpine
        ports:
        - containerPort: 80
        env:
        - name: MY_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: MY_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        command: ["/bin/sh", "-c"]
        args:
          - |
            envsubst '$MY_POD_NAME $MY_POD_IP $MY_NODE_NAME' < /tmp/templates/index.html > /usr/share/nginx/html/index.html
            echo 'server { listen 80; location / { root /usr/share/nginx/html; add_header Connection "close" always; add_header Cache-Control "no-store" always; } }' > /etc/nginx/conf.d/default.conf
            nginx -g 'daemon off;'
        volumeMounts:
        - name: demo-template
          mountPath: /tmp/templates
      volumes:
      - name: demo-template
        configMap:
          name: kina-demo-content

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kina-demo-content
  namespace: default
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Kina Demo Success!</title>
        <style>
            * { margin: 0; padding: 0; box-sizing: border-box; }
            body {
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                display: flex;
                align-items: center;
                justify-content: center;
            }
            .container {
                background: white;
                padding: 40px;
                border-radius: 20px;
                box-shadow: 0 20px 40px rgba(0,0,0,0.1);
                text-align: center;
                max-width: 600px;
                margin: 20px;
            }
            .header {
                color: #2c3e50;
                margin-bottom: 30px;
                font-size: 2.5rem;
                font-weight: 300;
            }
            .success-badge {
                display: inline-block;
                background: #2ecc71;
                color: white;
                padding: 8px 16px;
                border-radius: 20px;
                font-size: 0.9rem;
                margin-bottom: 20px;
                font-weight: 500;
            }
            .info-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 20px;
                margin: 30px 0;
            }
            .info-card {
                background: #f8f9fa;
                padding: 20px;
                border-radius: 10px;
                border-left: 4px solid #3498db;
            }
            .info-label {
                font-weight: 600;
                color: #34495e;
                margin-bottom: 5px;
                text-transform: uppercase;
                font-size: 0.8rem;
                letter-spacing: 1px;
            }
            .info-value {
                font-size: 1.1rem;
                color: #2c3e50;
                font-family: 'Monaco', 'Menlo', monospace;
            }
            .pod-info {
                background: #eaf2fd;
                border: 1px solid #3498db;
                border-radius: 8px;
                padding: 15px;
                margin: 20px 0;
            }
            .pod-info .info-grid {
                margin: 10px 0 0 0;
            }
            .features {
                text-align: left;
                margin: 30px 0;
            }
            .feature {
                display: flex;
                align-items: center;
                margin: 10px 0;
                padding: 10px;
                background: #ecf0f1;
                border-radius: 8px;
            }
            .feature-icon {
                margin-right: 15px;
                font-size: 1.2rem;
            }
            .footer {
                margin-top: 30px;
                padding-top: 20px;
                border-top: 1px solid #ecf0f1;
                color: #7f8c8d;
                font-size: 0.9rem;
            }
            .cluster-info {
                background: #e8f5e8;
                border: 1px solid #2ecc71;
                border-radius: 8px;
                padding: 15px;
                margin: 20px 0;
            }
            @media (max-width: 600px) {
                .info-grid { grid-template-columns: 1fr; }
                .container { padding: 20px; margin: 10px; }
            }
        </style>
    </head>
    <body>
        <div class="container">
            <div class="success-badge">&#x2705; DEPLOYMENT SUCCESSFUL</div>
            <h1 class="header">&#x1F389; Kina Demo Cluster</h1>

            <div class="cluster-info">
                <div class="info-label">Cluster Name</div>
                <div class="info-value">${CLUSTER_NAME}</div>
            </div>

            <div class="pod-info">
                <div class="info-label">Served By</div>
                <div class="info-grid">
                    <div class="info-card">
                        <div class="info-label">Pod Name</div>
                        <div class="info-value">${MY_POD_NAME}</div>
                    </div>
                    <div class="info-card">
                        <div class="info-label">Pod IP</div>
                        <div class="info-value">${MY_POD_IP}</div>
                    </div>
                    <div class="info-card">
                        <div class="info-label">Node</div>
                        <div class="info-value">${MY_NODE_NAME}</div>
                    </div>
                    <div class="info-card">
                        <div class="info-label">Ingress</div>
                        <div class="info-value">nginx-ingress</div>
                    </div>
                </div>
            </div>

            <div class="info-grid">
                <div class="info-card">
                    <div class="info-label">Platform</div>
                    <div class="info-value">Apple Container</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Replicas</div>
                    <div class="info-value">4 pods</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Domain</div>
                    <div class="info-value">${CLUSTER_NAME}-control-plane.${DNS_DOMAIN}</div>
                </div>
                <div class="info-card">
                    <div class="info-label">Load Balanced</div>
                    <div class="info-value">Yes (refresh to see)</div>
                </div>
            </div>

            <div class="features">
                <div class="feature">
                    <span class="feature-icon">&#x1F3D7;&#xFE0F;</span>
                    <span>Kubernetes cluster running on Apple Container</span>
                </div>
                <div class="feature">
                    <span class="feature-icon">&#x1F310;</span>
                    <span>NGINX Ingress Controller (nginx.org) installed</span>
                </div>
                <div class="feature">
                    <span class="feature-icon">&#x1F4E6;</span>
                    <span>Demo application with 4 replicas deployed</span>
                </div>
                <div class="feature">
                    <span class="feature-icon">&#x1F517;</span>
                    <span>Ingress routing configured and working</span>
                </div>
            </div>

            <div class="footer">
                <strong>&#x1F680; Ready for development!</strong><br>
                Your Kina cluster is fully configured and ready to use.
                <br><br>
                <em>Generated by: mise run test:cluster</em>
            </div>
        </div>
    </body>
    </html>

---
apiVersion: v1
kind: Service
metadata:
  name: kina-demo-service
  namespace: default
  labels:
    app: kina-demo-app
spec:
  selector:
    app: kina-demo-app
  ports:
  - name: http
    port: 80
    targetPort: 80
  type: ClusterIP

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kina-demo-ingress
  namespace: default
  annotations:
    nginx.org/ssl-redirect: "false"
    nginx.org/server-tokens: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: ${CLUSTER_NAME}-control-plane.${DNS_DOMAIN}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kina-demo-service
            port:
              number: 80
